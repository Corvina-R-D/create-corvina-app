{{- if eq .Values.gateway.appHost "[| .Name |].corvina.mk" }}

WARNING!!!
In order for the the app to be installed, add this line to your /etc/hosts (the windows one if using WSL), then visit https://[| .Name |].corvina.mk :

10.96.43.1 [| .Name |].corvina.mk
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
{{- end }}
